{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "GET Post Schema",
  "description": "Schema for a single post returned by GET /posts/{id}",
  "type": "object",
  "required": ["userId", "id", "title", "body"],
  "properties": {
    "userId": {
      "type": "integer",
      "minimum": 1,
      "description": "ID of the user who owns the post"
    },
    "id": {
      "type": "integer",
      "minimum": 1,
      "description": "Unique post identifier assigned by the server"
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "Post title — must be a non-empty string"
    },
    "body": {
      "type": "string",
      "minLength": 1,
      "description": "Post content — must be a non-empty string"
    }
  }
}